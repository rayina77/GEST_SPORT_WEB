services:
  - type: web
    name: gest-sport
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: bash start.sh

    plan: free

    autoDeploy: true

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: gest-sport-db
          property: connectionString